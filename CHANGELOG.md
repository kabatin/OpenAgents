# 変更履歴

このファイルの書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に従います。

各リリースの配布物は [Releases](https://github.com/kabatin/OpenAgents/releases) にあります。

## [Unreleased]

### 追加

- **宿題の催促に出口** — 一度目の「あれどうなりました？」に返事が無いと
  `asked` のまま永久に残っていた。数日後（`proactive.homework.second_nudge_days`）
  にもう一度だけ聞き、それでも動かなければ（`close_days`）「流れたものとして
  手放す」と一言添えて追跡を終える。どの段でも直前に後続会話と、声かけ自体への
  👍❌リアクションを読み直し、済んでいれば黙って閉じる。既存DBには
  `asked_at` 列を自動追加し、声かけログの時刻で埋める
- **決定事項台帳の重複除去** — 同じ決定が語順違いで何度も保存され台帳が
  水増しされていた。保存時に直近7日の決定と文字2-gramで照合して弾く
  （`core/textsim.py`。イベント逆算の同一判定と共通化）。既存分は
  `python -m core.dedupe_decisions --apply` で掃除できる
- **議事録TODOの作業チャンネル推定** — 抽出したTODOが全件 議事録ch に
  紐づき、実際の作業chから見えなかった。直近60日に人間が発言したchを候補に
  「どのchの話か」を推定させ、判断できなければ議事録chへ倒す
- **リンク先投稿の添付も読む** — 本文中のメッセージリンク/IDが指す過去投稿の
  添付（PDF等）を、本文・リプライ先の添付と同様に読解対象へ含める。同じ
  サーバーの投稿に限り、取得失敗は黙って空扱い
- **リマインダー上限の設定化** — 1人あたりのアクティブ上限を
  エージェント設定 `reminder_max_active` で変えられる（既定30件）。
  ダッシュボードの「基本動作」からも変更可

## [0.1.2] — 2026-08-27

導入と更新の入口を用意し、自発機能に「様子を見てから口を挟む」層を足した
リリース。あわせて、切り出し元プロジェクトの固有情報をコードから外した。

### 追加

- **`openagents` コマンド** — `cli/` に同梱。`openagents setup` で導入から
  設定画面まで。`start` / `stop` / `status` / `update` / `where` を持つ。
  中身は薄いランチャで、依存はゼロ。venv の作成・依存の導入・画面の組み立ては
  従来どおり `start.py` が担当する。
  **npm への公開は見送り**（同名の先行プロジェクトが複数あり、いずれも
  スコープ付きで公開しているため。→ [docs/11-cli.md](docs/11-cli.md)）。
  当面は `cd cli && npm install -g .` で入れる
- **`openagents update [--restart]`** — `git pull --ff-only` で更新し、
  `start.py` の準備処理を呼び直す。手元に変更があるときは
  **退避も破棄もせずに止まる**（未追跡ファイルは数えない）
- **操作用APIの `POST /shutdown`** — 常駐プロセス（`run.py`）ごと行儀よく
  終わらせる窓口。従来は個々のBOTの再起動しかできず、更新しても `run.py` 自身が
  古いコードを抱えたまま残っていた。127.0.0.1 固定・POST のみは従来どおり
- **会話への自発介入（注意ループ）** — 会話が途切れたチャンネルを読み、担当や
  結論が決まらないまま流れた話題を0〜10点で採点。閾値以上を**数時間寝かせて
  から**、発言直前にその後の会話を読み直し、人間だけで解決していれば黙って
  取り下げる。8〜22時のみ・同一chは12時間あける・日次上限あり。既定はオフ＋シャドー
- **リアクションの記録** — 全リアクションを常時記録する `reactions` テーブル。
  「👍で完結」した会話が機械から見えず、宿題の掘り起こしなどが誤発動していた。
  過去分は `platforms/discord/backfill_reactions.py` で埋め戻せる
- **同僚としての一言（⑤colleague）** — v0.1.1 のシャドー実験からの昇格。
  情報ではなく場への参加が目的の発言。**事実や数字を述べたらコードが黙らせ**、
  120字超でも黙る。専用の日次上限1回を他の枠と別勘定で持つ。既定はオフ

### 修正

- 宿題の声かけ: 直前に後続の会話とリアクションを読み直し、完了済みなら
  掘り返さない。確認自体が失敗したときは従来どおり声かけへ倒す
- イベント逆算: 同日かつ言い回し違いの決定を重複提案しない。決定台帳が同じ
  決定を再抽出するたびに連投されていた
- A/B実験: ゲートを使用回数から評価件数へ。評価ゼロの変種が👍率0%扱いになり、
  相手に👍が1件付いただけで「差あり」と誤検知して2時間ごとに再送していた

### 変更

- **定型文から特定の口調を外した** — エージェントが投稿する静的文面に特定の
  語尾が294箇所焼き込まれており、ペルソナで別の口調を設定しても反映されなかった。
  コードが必ず付ける定型文は中立な丁寧語にし、LLMに書かせる文面は
  ペルソナを渡して口調をそこから取るようにした（注意ループの採点プロンプトに
  口調が直接書かれていたのを含む）
- **切り出し元の固有情報を外した** — 実在の人名・サービス名・商品の販売状況が、
  テストの固定値だけでなく一部の指示文（LLMに渡るプロンプト）にも残っていた。
  機構の説明として通じる中立な例に置き換えた

### ドキュメント

- README にヒーロー画像・「何を解決するのか」章・構成図（Mermaid）を追加
- `docs/11-cli.md` — コマンド一覧と、置き場がどう決まるか
- Issue / PR テンプレート、行動規範、この変更履歴を追加

テスト 1302件（core 751 / platforms 369 / dashboard 138 / cli 44）、
macOS・Windows・Linux で緑。

## [0.1.1] — 2026-08-21

「言ったことを実際にやる」まわりを厚くし、v0.1.0 のあと実運用で見つかった穴を塞いだリリース。

### 追加

- **事実台帳（FACT）** — 「いま実際どうなっているか」を会話から記録する。同じ主題の古い認識は自動で上書きされ、回答時に最優先の一次資料として引かれる。従来は指針・決定・用語のどれでもない「事実の現状」に受け皿が無く、訂正されても何も残らなかった
- **納期追跡の期日変更** — 追跡中のタスクを会話からリスケできる。期日を変えると事前の声かけもやり直す（従来は完了・キャンセルのみ）
- **心がけの卒業制度** — 週次で蒸留される「心がけ」が3週続いたら、定着した課題として恒久ルールへの格上げを提案する（✅ で昇格）。蒸留の入力も5系統（低評価・👎・裏取りなしの断定・できたフリ・差し止め）へ拡大
- **「その他」枠の観察実験** — 発言する類型の枠外で「同僚なら一言添える」と判断した場面を、投稿せず記録だけする。枠の絞り込みが妥当かをダッシュボードの「学びと実験」タブで人間が判断できる
- **名前辞書タブ / 学びと実験タブ** — 固有名詞辞書・単語帳と、いま効いている心がけをダッシュボードから閲覧できる
- **初期化（危険な操作）** — 会話ログと設定をダッシュボードから消せる。別サーバーへの繋ぎ替えは、前のサーバーの会話が混ざる前に起動を止める

### 修正

- 「認識を更新します」のような空約束も、できたフリとして検出するようにした
- 同じチャンネルの過去ログが検索対象から漏れていた
- 前提知識（`knowledge/`）が読まれず、消すこともできなかった
- A/B実験が一度も起動していなかった（変種の種撒きがどこにも配線されていなかった）
- ゴールデンセットに一方的な投稿（週次レポート等）が混ざり、Q&Aペアが壊れていた
- まっさらな Mac で踏む2つの起動不能
- 議事録BOTの対象ボイスチャンネルをID参照に変更（改名すると静かに録音が止まっていた）
- `claude` が入っていない環境でテストが落ちる問題、Actions の Node.js 20 非推奨警告

### 変更

- 新しい回答経路（runner）と会話の継続を既定 ON にした

### 移行時の注意

- 議事録BOTを使っている場合、`config.json` の `meeting_bot.voice_channel_name` は
  **`voice_channel_id`** に変わりました。ダッシュボードの「議事録BOT」から、
  チャンネルを右クリック →「IDをコピー」で取得したIDを設定し直してください

テスト 1216 件（core 709 / platforms 369 / dashboard 138）、macOS・Windows・Linux で緑。

## [0.1.0] — 2026-08-14

チャットに住みつくAIエージェント基盤の、最初の公開リリース。

### 追加

- **ブラウザだけで完結するセットアップ** — 設定ファイルを開かずに、Discord でエージェントが挨拶するまで
- **過去ログ検索回答** — 全会話をローカル SQLite に記録し、引用リンク付きで回答
- **30種の観察ループ** — 納期の声かけ・宿題追跡・週次レポートなど。1日の発言上限とシャドーモード付き
- **できたフリ検出** — 「やりました」と実行結果のズレを機械的に検出し、同じ投稿内で訂正
- **内蔵スーパーバイザ** — クラッシュ自動再起動・ハング検出・ログローテーション
- **macOS / Windows 対応** — CI で3OS検証済み（テスト 1117 件）
- **LLM差し替え** — Claude Code / Codex CLI / 自前の CLI

### 既知の制限

- 動くプラットフォームは Discord のみ（Slack / LINE / Telegram はインターフェースと手順書のみ）
- 添付読解・Web検索などツールを使う機能は Claude Code 選択時のみ
- ドキュメントは日本語が正（英語は README のみ）

[Unreleased]: https://github.com/kabatin/OpenAgents/compare/v0.1.2...HEAD
[0.1.2]: https://github.com/kabatin/OpenAgents/compare/v0.1.1...v0.1.2
[0.1.1]: https://github.com/kabatin/OpenAgents/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/kabatin/OpenAgents/releases/tag/v0.1.0
