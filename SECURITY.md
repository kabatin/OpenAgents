# セキュリティについて

## 脆弱性を見つけたら

**公開のIssueには書かないでください。** リポジトリの Security タブから
非公開で報告してください（GitHub の private vulnerability reporting）。

## この仕組みが扱う秘密

| もの | どこにあるか | 守り方 |
|---|---|---|
| Botトークン | `config.json`（gitignore対象） | 画面には表示しない（保存はできるが読み出せない） |
| 会話の記録 | `state/archive.db` | 手元にのみ保存。外へ送らない |
| AIの認証情報 | 各CLIツールが持つ | この仕組みは触らない |

## 設計上の判断

**管理画面は既定で `127.0.0.1` にのみ待ち受けます。**
この画面はBotトークンを読めるプロセスなので、外に出すのは明示的な選択に
してあります。LANに出すときにパスワードが未設定なら警告します。
**インターネットには公開しないでください。**

**常駐プロセスの操作APIは `127.0.0.1` 固定で、設定でも変えられません。**
ここを外に出すと、誰でもBOTを止められてしまいます。

**状態を変えるAPIはすべて POST 系です。** GETで副作用を作らないので、
悪意あるページに画像タグを踏まされてもBOTは再起動しません。

**添付ファイルを読ませるとき、AIのファイル読み取りを一時フォルダに
閉じ込めています。** ホーム配下の読み取りは二重に禁止しています
（プロンプトインジェクションで `~/.ssh` を読ませる、を防ぐため）。

**AIが自分で生成する道具（`core/tools/`）は、`os` / `subprocess` /
`socket` の import を構文解析で禁止しています。** ネットワークを使う連携は
人間が書く `integrations/` 側にしか置けません。

## 承知しておいてほしいこと

- **質問と検索結果は、あなたが選んだAIサービスに送られます。**
  機微な情報を扱う場合は、そのサービスの規約を確認してください
- `core/tools/` のプラグイン機構は**完全なサンドボックスではありません**。
  多層の防御はしていますが、信用できないコードを置かないでください
- 開発BOT（既定オフ）は、承認を経てリポジトリのコードを書き換えます。
  何を許すかは `platforms/discord/dev/dev_gate.py` で決まります
